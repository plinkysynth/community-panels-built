# Looper

An 8-track touch looper backed by PSRAM.

Eight track rows share the surface with per-track controls. Hold record and tap
a row to arm it; stopped recording starts from input level, while playing can
sync to a loop head crossing.

Playback pads launch transient heads. Hold tap tempo as shift to edit the
latched loop head.
